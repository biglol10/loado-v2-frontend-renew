import { Box, Typography } from '@mui/material';
import Home from '../home';

const AdminDashboard = () => {
  return (
    <Box>
      <Typography>This is admin dashboard page</Typography>
    </Box>
  );
};

export default AdminDashboard;
